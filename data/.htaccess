# Block all direct HTTP access to this folder (the SQLite database lives
# here). Works on Apache 2.4 and 2.2 — virtually every shared PHP host.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
